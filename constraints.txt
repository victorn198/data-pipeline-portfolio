# Security constraints for transitive dependencies.
#
# Install with:
#   pip install -r requirements.txt -c constraints.txt
#
# Keep this file narrow. Only pin transitive dependencies that are actually
# required to keep the project environment reproducible and audit-clean.

deepdiff==8.6.2

# dbt-core 1.10 maintenance-line compatibility set. These pins prevent pip
# from selecting incompatible adapter/protobuf combinations during resolution.
dbt-adapters==1.24.4
dbt-common==1.38.0
dbt-extractor==0.6.0
dbt-protos==1.0.541
dbt-semantic-interfaces==0.9.0
mashumaro==3.14
protobuf==6.33.6
